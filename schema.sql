CREATE TABLE "ages" (
    "id" INTEGER,
    "name" TEXT NOT NULL,
    "start_year" INTEGER NOT NULL,
    "end_year" INTEGER,
    "description" TEXT,
    PRIMARY KEY("id")
);

CREATE TABLE "campaigns" (
    "id" INTEGER,
    "title" TEXT NOT NULL,
    
)
